Define:
<%= print_separate_line %>
static Vector<<%= name.capitalize %>*> * items;

Initialize:
<%= print_separate_line %>
Vector<<%= name.capitalize %>*> *<%= class_name.capitalize %>::items = new Vector<<%= name.capitalize %>*>();

Using: 
<%= print_separate_line %>
items->pushBack(item);

<%= name.capitalize %> *item;
for (auto current = items->begin(); current != items->end(); current++) {
  item = *current;
  if (item->getBoundingBox().containsPoint(touch->getLocation())){
  }
}

items->eraseObject(item);
